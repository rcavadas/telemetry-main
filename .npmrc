# NPM Configuration
# Note: This project is designed to use pnpm (see CURSOR.md)
# This .npmrc is provided as a fallback for npm users

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true

# Alternative: if legacy-peer-deps doesn't work, try:
# strict-peer-deps=false
